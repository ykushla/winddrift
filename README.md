# Wind Drift v0.9.1

Patch release: footer version is now rendered from `src/version.js` instead of hard-coded HTML.


Static, mobile-first wind-correction prototype designed for later deployment on GitHub Pages.

## v0.9 changes

- Intermediate coordinates are shown side-by-side for faster field reading.
- In `%` mode, the percentage and calculated distance in metres are both large and on the same line.
- In `м` mode, the entered distance and calculated percentage are both large and on the same line.
- Calculation logic is unchanged from v0.9.


## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed by the server.

## Test

```bash
npm test
```

## GitHub Pages

Publish the `main` branch from `/ (root)` in **Settings → Pages**. The root `index.html` redirects to `./public/`.


## v0.9
- Більші дублюючі координати м/% поруч з основним значенням.
- Дублююче значення оновлюється на кожному input/русі повзунка.
- Footer з версією та посиланням RangeRabbit.


## v0.9
Проміжні точки одночасно мають повзунок 0–100% і поле дистанції в метрах. Керування синхронізоване в обидва боки; останній змінений тип координати лишається внутрішнім джерелом позиції при зміні дистанції цілі.

## v0.10.2
- Мінімальна дистанція цілі: 100 м.
- Кожна проміжна точка мусить бути щонайменше на 1 м далі за попередню і на 1 м ближче за наступну.
- Межі повзунків позиції перераховуються автоматично за сусідніми точками.
- Ручне введення метрів автоматично обмежується `попередня + 1 м` / `наступна - 1 м`.


## v0.10.2

- Ручне введення позиції в метрах тепер комітиться лише по Enter або втраті фокусу.
- Під час набору поле не викликає валідацію і не перераховує позицію.
- Повзунок у %, а також крокові стрілки числового поля зберігають миттєву реакцію.


## v0.10.2
- Дистанція цілі обмежена максимальною дистанцією активної імпортованої AB Range Card.
- Максимум береться з останнього `Range` у CSV і застосовується як у UI, так і в state-логіці.


## v0.12.0
- Дистанція цілі застосовується лише після Enter або втрати фокусу; стрілки ↑/↓ лишаються миттєвими.
- Без імпортованого профілю діапазон дистанції цілі зафіксований на 100 м.
- Верхнє меню згорнуте до hamburger-іконки справа.
- У меню додано перемикач світлої/темної теми з локальним запам’ятовуванням.
- В інструкції рекомендовано крок Range Card 5 м та прибрано дубль попередження про горизонтальні ефекти.


## v0.13.0
- Посилено валідацію Applied Ballistics CSV.
- Помилки: відсутній/некоректний MV, Range Card коротший за 100 м, некоректні або непослідовні рядки, непридатний crosswind.
- Попередження: крок, відмінний від рекомендованих 5 м, великий крок (>10 м), нерівномірні прогалини та пізній початок таблиці.
- В інструкції рекомендовано генерувати Range Card з кроком 5 м та сильнішим боковим вітром 10 м/с.


## v0.13.1
- Бренд WIND DRIFT перенесено в один верхній рядок із hamburger-меню.
- Назву та іконку збільшено для кращої читабельності.
- Дубль бренду над профілем прибрано для економії вертикального простору.


## v0.14.0
- У «Довідник» додано окрему сторінку «Читання вітру по міражу».
- Додано метричну класифікацію міражу: 0; 0.5–1.3; 1.8–3.1; 3.6–5.4 м/с.
- Додано власні адаптивні SVG-ілюстрації типів міражу.
- Додано блоки про кут хвиль, напрям руху, щільність/видимість міражу та практичне попередження.
- Довідник тепер має окрему стартову сторінку, готову для майбутніх статей.


## v0.14.1
- Після успішного імпорту CSV повідомлення про імпорт і нагадування щодо Spin Drift/Coriolis більше не показуються.
- Заголовки крайніх точок уніфіковано зі стилем проміжної дистанції.
- «Позиція» перейменовано на «Дистанція».
- Верхнє поле «Дистанція цілі» перейменовано на «Ціль».
